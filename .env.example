# Copy this file to ".env" and paste your real Neon connection string.
# .env is gitignored, so your real credentials never get committed.
NEON_DSN=postgresql://USER:PASSWORD@ep-xxxx.neon.tech/dbname?sslmode=require
