# never commit secrets or local junk
.env
venv/
.venv/
__pycache__/
*.pyc

# dbt artifacts (generated locally / by CI, never source-controlled)
target/
dbt_packages/
logs/
profiles.yml

# Streamlit secrets (real DSN lives here locally; never commit it)
.streamlit/secrets.toml
